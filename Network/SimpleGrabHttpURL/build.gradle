apply plugin: 'application'

application {
    mainClassName = 'SimpleGrabHttpURL'
    description = "SimpleGrabHttpURL Example"
}
if(!project.hasProperty('site')){
  ext.site = 'https://devhints.io/bash'
}
run {
  // default arguments
  args project.site // url
}
